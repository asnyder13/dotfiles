#!/usr/bin/env -S ruby -w
# frozen_string_literal: true
# typed: false

require 'date'

abort 'Must to provide a time' if ARGV[0].nil?
hours = ARGV[0]&.to_i
abort 'Must to provide a time' unless hours
puts (Time.now - 60 * 60 * hours).to_datetime.rfc2822
